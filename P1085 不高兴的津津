#include<stdio.h>
int main() {
	int a,b,t,i,max,week;
	max = 0;
	for(i = 1; i <= 7; i++) {
		scanf("%d %d", &a, &b);
		
		t = a + b;
		if(t > max) {
			max = t;
			week = i;
		}	
	}
		if(max > 8) {
			printf("%d",week);	
			
			} else{
			printf("0");
	    }
	return 0;
}
